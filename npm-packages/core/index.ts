import {
  type JournalEvent,
  parseWithBigInt,
} from "@elite-dangerous-plugin-framework/journal";

/**
 * Inherit from this class for your Plugin definition.
 */
export abstract class EDPFPluginElement implements HTMLElement {
  constructor() {}
}
